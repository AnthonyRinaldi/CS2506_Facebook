#ifndef USERS
#define USERS
/*
struct user
{
	char* name;
	char* location;
	int gender;
	int age;
};

int adduser(char* name, char* location, int gender, int age);
void removeuser(int);
*/
#endif USERS
